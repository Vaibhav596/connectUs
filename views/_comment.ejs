<li>
    <% if(locals.user && locals.user.id == comment.id){ %>
        <a href="/comments/destroy/<%= comment.id%>">Delete</a>
    <% } %>
    <p>
        <%= comment.content %>
        <br>
        <small>
            <%=comment.user.name%>
        </small>
       
    </p>
</li>